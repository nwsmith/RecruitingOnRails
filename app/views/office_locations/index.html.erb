<h1>Listing office_locations</h1>

<table>
  <tr>
    <th>Code</th>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @office_locations.each do |office_location| %>
  <tr>
    <td><%= office_location.code %></td>
    <td><%= office_location.name %></td>
    <td><%= office_location.description %></td>
    <td><%= link_to 'Show', office_location %></td>
    <td><%= link_to 'Edit', edit_office_location_path(office_location) %></td>
    <td><%= link_to 'Destroy', office_location, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Office location', new_office_location_path %>
