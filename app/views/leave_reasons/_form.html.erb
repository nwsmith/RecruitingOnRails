<%= form_for(@leave_reason) do |f| %>
  <% if @leave_reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leave_reason.errors.count, "error") %> prohibited this leave_reason from being saved:</h2>

      <ul>
      <% @leave_reason.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
