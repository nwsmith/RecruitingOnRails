<%= month_calendar events: @candidates do |day, candidates| %>
    <%= day.day %>
    <br/>
    <% candidates.each do |candidate| %>
        <% if day >= candidate.start_date %>
            <%= candidate.name %> (<%= candidate.start_date.year %>) <br/>
        <% end %>
    <% end %>
<% end %>