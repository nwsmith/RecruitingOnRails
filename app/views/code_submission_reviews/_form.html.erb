<%= form_for(@code_submission_review) do |f| %>
    <% if @code_submission_review.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@code_submission_review.errors.count, "error") %> prohibited this code_submission_review
            from being saved:</h2>

          <ul>
            <% @code_submission_review.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :code_submission_id %>
      <%= f.collection_select(:code_submission_id, CodeSubmission.all, :id, :name) %>
    </div>
    <div class="field">
      <%= f.label :user_id %>
      <%= f.collection_select(:user_id, User.all, :id, :name) %>
    </div>

    <div class="field">
      <%= f.label :approved %>
      <br/>
      <%= f.check_box :approved %>
    </div>
    <div class="field">
      <%= f.label :notes %>
      <br/>
      <%= f.text_field :notes %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
